import { RawTypesConfig } from '@graphql-codegen/visitor-plugin-common';
/**
 * @description This plugin generates the base Dart types, based on your GraphQL schema.
 *
 * The types generated by this plugin are simple, and refers to the exact structure of your schema
 */
export interface DartPluginConfig extends RawTypesConfig {
    mappers?: boolean;
}
